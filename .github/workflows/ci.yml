name: CI

on:
  push:
  pull_request:

jobs:
  crypto-core:
    name: crypto-core — blocking (TESTING.md §1)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Install libsodium
        run: brew install libsodium
      - name: Run crypto suite
        working-directory: crypto-core/swift
        run: swift run CryptoCoreTests

  ios-app:
    name: iOS app shell builds (simulator)
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build for iOS Simulator
        working-directory: ios
        run: >
          xcodebuild -project Souvenir.xcodeproj -scheme Souvenir
          -sdk iphonesimulator -configuration Debug
          CODE_SIGNING_ALLOWED=NO build

  # ── PENDING ───────────────────────────────────────────────────────────────
  # invariants:
  #   name: invariants — blocking (TESTING.md §2)
  #   runs-on: ubuntu-latest
  #   needs the backend (`backend/`). Asserts, per route:
  #     - no endpoint returns cleartext (only blobs / wrapped keys / opaque metadata)
  #     - no endpoint accepts content without a wrapped key
  #     - grep the bundle for embedded secrets / test keys / backdoors (SECURITY §1.7)
  #     - no vault content in logs or scrubbed stack traces
  #     - no civil date / tag in cleartext server-side (SECURITY §1.4, §6.2)
